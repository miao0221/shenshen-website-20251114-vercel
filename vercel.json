{
  "routes": [
    // 静态资源文件直接返回
    {
      "src": "/(.*\\.(css|js|jpg|png|svg|ico))",
      "dest": "/$1"
    },
    // 所有其他路径回退到 index.html (用于 SPA 路由)
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}